# 项目上下文（加载模板）

将智能体加载到具体工程时，用该工程真实信息替换下方占位，使通用方法论对齐实际项目。

## 工程结构

```
<your-project>/
├── <模块A>      <职责 / 技术>
├── <模块B>      <职责 / 技术>
└── <共享包 / 配置>
```

## 技术栈

| 层 | 技术 |
|----|------|
| 前端 | <如 Web / 小程序 / 桌面> |
| 后端 | <如 服务框架 / 语言> |
| 存储 | <如 数据库 / 文件系统> |
| 部署 | <如 容器 / 静态托管> |

## 设计常量

- 主色 / 背景 / 文字：<色值>
- 风格约束：<如 不使用渐变>
- 品牌元素：<如 Logo 规范>

## 启动与验证命令

```bash
# 列出该工程本地启动、构建、验证的关键命令
```

## 通用原则（适用于任何项目）

- 同类修改必须扫全量，不只在手头文件改。
- 横切关注点（校验 / 日志 / 配置）统一收口，禁止各端拷贝。
- 对外契约变更需同步更新 spec 与版本记录。
