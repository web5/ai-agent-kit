# 基线重置声明 · 2026-09-10

> 命名说明：常规报告 ID 为 `<kit-commit短hash>-<日期>`。本文件是**换基线声明**而非分数基线，故以 `baseline-reset-<日期>` 标识，避免被误读为已评分基线；对应 kit commit 记在头部信息（`ec87f1c`）。文件落盘后不改写内容。
>
> 本文件是**换基线事件**的正式记录，不是分数基线：它声明旧基线作废、新基线定义冻结，并明确分数采集状态。原基线 `6373b99-2026-09-10.md` 保持原样、不修改、不删除。

## 头部信息

| 项 | 值 |
|----|----|
| kit commit | `ec87f1c`（本文件与该变更同批提交） |
| 变更摘要 | 资产模型再设计：撤销 `kits/`（L1/L2/L3 三个常驻 kit）与相关门禁，产出纪律常驻 `AGENT.md`、工程纪律并入既有技能实体、设计论证迁 `references/methodology-design.md`；并修计数/编号/路径口径缺陷 |
| 被测模型 | 未采集（见「采集状态」） |
| 温度 / 采样 | 未采集 |
| 每任务运行次数 | 未采集 |
| judge 模型 | 未采集 |
| 人工复检比例 | 未采集 |
| 评测人 | `<待填>` |
| 日期 | 2026-09-10 |

## 为什么重置（触发依据）

`references/eval-framework.md` §4 防漂移四闸之第 2 闸：**改任务卡输入或期望输出 = 换基准 = 基线重置**。本次命中三项：

1. **期望输出变更**：`evals/cases/routing.md` R24–R28 的期望分派由 `L1-karpathy` / `L2-superpowers` 改为产出纪律与 `rd-execute` / `rd-digital-agent` / `rd-review` 的新落点（用例**输入**未动）；
2. **判定对象变更**：`AGENT.md` 结构改写（新增 §产出纪律、§工程纪律，删「方法论的三个组成部分」），常驻加载集从 `AGENT.md + kits/*/SKILL.md + skills/*/SKILL.md` 变为 `AGENT.md + skills/*/SKILL.md`；
3. **结构门禁变更**：`check-structure.sh` 移除 S9，编号收敛为 S1–S8。

因此旧基线 `6373b99` 的分数**仅在旧定义下有效**，不可与新定义横向对比。

## 新基线定义（冻结件）

| 冻结件 | 新定义 | 状态 |
|---|---|---|
| L1 结构 | `scripts/check-structure.sh` S1–S8 | ✅ 已核验通过（见下） |
| L2 路由用例 | `evals/cases/routing.md` **28 条**（含 R24–R28 新期望） | 定义已冻结，分数未采集 |
| L3 陷阱用例 | `evals/cases/behavior.md` **7 条**（B1–B7） | 定义已冻结，分数未采集 |
| L4 任务卡 | `evals/golden-tasks/` T1–T6 + `RUBRIC.md` | 定义已冻结，分数未采集 |
| 评分内核 | `evals/golden-tasks/RUBRIC.md`（五维 D1–D5，通过线 ≥11 且 D1≥2 且 D4≥2） | 未改动，沿用 |
| 路线分支数 | Hub 分派决策树 11 分支 | 已核对 |

> 冻结后的用例输入与期望输出，此后**只增不改**；再改即再次触发重置。

## 分数表（未采集）

| 任务 | D1 产物落盘 | D2 流程遵循 | D3 人审节点 | D4 产出质量 | D5 上下文工程 | 总分 | 通过(≥11且D1≥2且D4≥2) |
|------|------------|------------|------------|------------|--------------|------|------|
| T1 brainstorm | — | — | — | — | — | — | ☐ |
| T2 plan | — | — | — | — | — | — | ☐ |
| T3 execute | — | — | — | — | — | — | ☐ |
| T4 debug | — | — | — | — | — | — | ☐ |
| T5 review | — | — | — | — | — | — | ☐ |
| T6 full-loop | — | — | — | — | — | — | ☐ |
| **均分** | — | — | — | — | — | — | **通过率 —/6** |

**采集状态：未采集**。原因：本机 `AGENT_CMD` 为空（`.env` 第 8 行为空值）、PATH 无无头 agent CLI，`scripts/run-eval.sh:52` 硬退出；且本机不满足「干净上下文 + judge 隔离」的基线前提。

**明确声明**：本表不含任何数值。⚠️ 不得将本文件当作「已评测通过」的证据，也不得据此判定退化或改善。

## 已完成的机器层核验（可复现）

| 项 | 结果 |
|---|---|
| `bash scripts/check-structure.sh` | 通过（S1–S8），exit 0 |
| S7 三处 must-keep 字面串（`验证判据表` / `完成验证门` / `唯一方法论来源`） | 在位 |
| 旧概念残留（`kits/` / `three-kits` / `L1-karpathy` / `L2-superpowers` / `L3-anthropic` / `S1~S9`） | 零残留（冻结历史目录除外） |
| 跨目录引用路径可达性 | 5/5 可解析 |
| L1 结构用例清单与脚本一致性 | `evals/cases/structure.md` 与 `check-structure.sh` 同步（S1–S8） |

## 与上一版对比

**不可比**（定义已变更，非同口径）。按 §4 防漂移四闸，重置后的第一份分数报告将与更早的 `6373b99` 构成**跨定义对比**，只能标注为「换基线后首跑」，不得作为趋势线的直接延续。

| 指标 | 上一版（`6373b99`，旧定义） | 本版 | Δ | 判定 |
|------|-----------------|------|----|----|
| 通过率 | 6/6（旧用例集与旧期望） | 未采集 | N/A | 不可比 |
| D1~D5 各维均分 | 13.2（均分） | 未采集 | N/A | 不可比 |
| 产物落盘率 | 14%（机器口径失真，见旧报告） | 未采集 | N/A | 不可比 |
| 红线违规数 | 0 | 未采集 | N/A | 不可比 |

## 补跑方式（新基线采集）

1. 准备可无头调用的被测 agent，按 `evals/run-baseline.md`「脚本化执行」设置 `AGENT_CMD`（占位符 `{input_file}` `{workspace}` `{output_file}`）；
2. 固定模型与温度，每任务 2~3 次取均值，judge 与被测异会话，人工复检 ≥20%；
3. 编排：`bash scripts/run-eval.sh`；产物核对：`bash scripts/check-artifacts.sh`；报告骨架：`bash scripts/gen-report.sh`；
4. 报告落盘 `evals/reports/<commit短hash>-<日期>.md`，头部「变更摘要」注明「换基线后首跑」，并与本文件交叉引用；
5. 采集完成前，任何行为层变更**不得**以「已评测」名义合并（见 `references/methodology-design.md` §十）。

## 结论与下一轮 intent

- 基线状态：**已重置，定义冻结，分数待采集**；
- 合并判定：本次改动以「结构层已核验 + 行为层基线待采」处置；若门禁要求报告，以本文件作为换基线声明附上，并在 PR 描述说明行为层未采集的原因；
- 下一轮 intent：① 在具备 `AGENT_CMD` 的环境完成新基线采集；② 采集结果若暴露 `AGENT.md` §产出纪律 / §工程纪律 的触发性问题，回灌为下一轮 `AGENT.md` 与技能实体的调整。
