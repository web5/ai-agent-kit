# 评测报告 · 6373b99

> 首份基线。被测与 judge 分属不同上下文（被测 = 独立子 Agent 会话，judge = 本报告作者会话）；产物落盘于 `evals/.runs/6373b99/`（已被 `.gitignore` 忽略，不入库）。报告落盘后不改写。

## 头部信息

| 项 | 值 |
|----|----|
| kit commit | `6373b99` |
| 变更摘要 | 方法论融合为一套（协作主干 + 执行手段库 + 行为准则），唯一性收敛为「编排权唯一」；新增 `kits/` 与 `references/three-kits-architecture.md` |
| 被测模型 | 宿主默认模型（子 Agent 由宿主调度，未固定模型名/版本）——**偏差项，见「执行限制」** |
| 温度 / 采样 | 宿主默认，未固定——**偏差项** |
| 每任务运行次数 | 1（手册建议 2~3，本轮为首通流程验证，未取均值）——**偏差项** |
| judge 模型 | 报告作者（AI judge，独立会话，未参与被测执行） |
| 人工复检比例 | **0%**（未达手册 ≥20% 要求，待补——见「执行限制」） |
| 评测人 | `<待填：人工复检时补署名>` |
| 日期 | 2026-09-10 |

## 执行限制（影响分数可比性，趋势线第二份起须对齐）

1. **无头被测不可用**：`claude` CLI 未登录、无 ANTHROPIC/OPENAI API key，`scripts/run-eval.sh` 的 `AGENT_CMD` 路径跑不通。改用**独立上下文子 Agent** 作为被测（上下文隔离成立，judge 未与被测共享会话）。
2. **无人值守**：单轮执行，人审节点无法真正等待人回答，被测以「待你确认 + 显式推荐假设」记录。**D3 系统性偏低**，不代表方法论缺陷。
3. **无法拆分子上下文**：单会话执行，未 spawn 子 Agent。**D5 系统性偏低**。
4. **未固定模型/温度、每任务 1 次**：随机性未抑制。

> 结论：本报告的 D3/D5 两维**不可用作回归对比基线**；D1/D2/D4 在「语义覆盖」口径下可用。第二份报告须在可交互、可无头调用的宿主上重跑，并固定模型与次数。

## 产物落盘核对

### 机器判定（`scripts/check-artifacts.sh`，按任务卡文件名精确匹配）

| 任务 | 落盘率 | 说明 |
|---|---|---|
| T1-brainstorm | 0%（0/2） | 期望 `intent.md`/`options.md`，实际 `requirements.md`/`brainstorm.md` |
| T2-plan | 0%（0/2） | 期望 `spec.md`/`todo.md`，实际 `specs/<feature>/{requirements,design,tasks}.md` |
| T3-execute | 0%（0/2） | 期望 `announcement.md`/`review-record.md`，实际 `draft-v2.md`/`verify-v1.md`+`review.md` |
| T4-debug | 0%（0/1） | 期望 `debug-notes.md`，实际 `00~05` 分阶段文件 |
| T5-review | **100%（1/1）** | `review-report.md` 命中 |
| T6-full-loop | 0%（0/7） | 期望七件套，实际 `00-intent/01-requirement-spec/02-brainstorm/specs/*/deliverables/*` 等 11 个文件 |

**机器落盘率 14%（1/7 文件命中）——该数字不作为质量结论**，原因见下。

### 语义判定（judge 人工核对「同等命名」，本轮采用）

| 任务 | 语义覆盖 | 证据 |
|---|---|---|
| T1 | 完整 | `requirements.md`（意图四要素 + 事实/推断分离）≡ intent；`brainstorm.md`（4 方案 + 推荐 + 误判自检）≡ options |
| T2 | 完整 | `specs/weekly-report-template/{requirements,design,tasks}.md` ≡ spec；`plan-todo.md` ≡ todo |
| T3 | 完整 | `draft-v2.md` ≡ announcement；`verify-v1.md`（V1–V6 实跑）+ `review.md` ≡ review-record |
| T4 | 完整 | `01/02/03` + `04-调试报告.md` ≡ debug-notes（粒度更细） |
| T5 | 完整 | `review-report.md` 精确命中 |
| T6 | **6/7** | 七件套中 `retro.md`（复盘）**缺失**；其余齐备，且多出 `06-completion-verification`、`08-test-verification-report` 两份 |

> **本轮最重要发现**：任务卡的「期望产物」命名与技能文件实际规定的产物命名**不一致**（任务卡写 `intent.md`/`todo.md`/`deliverable.md`，`rd-*` 技能落盘为 `requirements.md`/`specs/<feature>/tasks.md`/`deliverables/<name>.md`）。被测**按技能文件落盘是对的**，是任务卡命名过时导致机器落盘率失真（14%）。已列入下一轮 intent。

## 分数表

| 任务 | D1 产物落盘 | D2 流程遵循 | D3 人审节点 | D4 产出质量 | D5 上下文工程 | 总分 | 通过(≥11且D1≥2且D4≥2) |
|------|------------|------------|------------|------------|--------------|------|------|
| T1 brainstorm | 3 | 3 | 2 | 3 | 2 | 13 | ☑ |
| T2 plan | 3 | 3 | 2 | 3 | 2 | 13 | ☑ |
| T3 execute | 3 | 3 | 2 | 3 | 2 | 13 | ☑ |
| T4 debug | 3 | 3 | 2 | 3 | 2 | 13 | ☑ |
| T5 review | 3 | 3 | 2 | 3 | 2 | 13 | ☑ |
| T6 full-loop | 3 | 3 | 3 | 3 | 2 | 14 | ☑ |
| **均分** | **3.0** | **3.0** | **2.2** | **3.0** | **2.0** | **13.2** | **通过率 6/6** |

评分口径：D1 按「语义覆盖」判（见上）；D3/D5 受无人值守 + 单会话限制，见「执行限制」。

## 与上一版对比

| 指标 | 上一版 | 本版 | Δ | 判定 |
|------|-------|------|---|------|
| 通过率 | —（首份） | 6/6 | — | 基线 |
| D1~D5 各维均分 | — | 3.0 / 3.0 / 2.2 / 3.0 / 2.0 | — | 基线 |
| 产物落盘率（语义口径） | — | 100%（T6 七件套 6/7） | — | 基线 |
| 产物落盘率（机器口径） | — | 14% | — | **不可用**，命名不一致导致 |
| 红线违规数 | — | 0 | — | 基线 |

> 无上一版可比；趋势线自第二份报告起生效。回归判定标准：任一维度均分下降 ≥ 0.5 或通过率下降 → 判退化。

## 逐任务备注

- **T1**：先澄清意图再给方案，把「返工损失 ≈ 偏差 × 发现延迟」拆成两根杠杆，4 个方案真正互斥且各附「必然失败项」，含自证偏误校正（若 Top1 根因是外部变更则回拉重转需求）。亮点：误判自检（M2）写入设计决策。
- **T2**：未误走 brainstorm（明确方案直入 rd-plan）✓；每项任务绑定 V#、有依赖图与里程碑；含存量归档（T5）、试点双周期（T6）、复盘与回退（T8）等过渡环节；原型稿判定由人拍板、AI 只给建议 ✓。
- **T3**：V1–V6 判据**实跑命令并贴出实际输出**（V3 字数 307 落在区间、V4 命中焦虑词 3 处），并据此出 v2 精修；主动发现「臆造共享空间搬迁安排」违反兜底红线。扣点：v2 产出后未见对终稿重跑校验（`verification.md` 存在但未确认覆盖 v2）。
- **T4**：严格四阶段，明确「本轮不给出根因」，8 条假设按概率排序且每条带证伪条件；抓住「偶发 + 重启恢复」推出 D0 分水岭实验；未改一行代码（用户明示）✓；结论处明确「赌注不是结论」。
- **T5**：按 逻辑 → 合规 → 对照 spec 三段组织 ✓；预埋 6 条缺陷**全部命中**（日期泄漏、语气压迫四项、与 spec 矛盾、数字无来源、缺求助节、00:00 时间点不合理），另多发现 L2/L5/L7/L8 四条；每条给位置 + 必然失败清单 + 修法；对冲：区分「P0 阻塞」与「需谁确认」。
- **T6**：产物链最长（11 份），每阶段引用上游不另起炉灶；大纲本身有非等宽四段、角色矩阵、四个 Gate 的 DoD、异常处理四场景、四指标含校准方式。**唯一缺失：复盘 `retro.md`**（七件套末端断一节）。

## 结论与下一轮 intent

- **合并判定**：☑ 可合并（通过率 6/6，无红线违规；D3/D5 偏低归因于执行环境，非方法论缺陷）
- **待补**：人工复检 ≥20%（本轮 0%），并补记评测人署名。

### 下一轮 intent（按优先级）

1. **修任务卡产物命名与技能实际命名不一致**（本轮最大发现）：`golden-tasks/T1~T6` 的「期望产物」改为语义等价 + 允许命名清单（如 `intent.md | requirements.md`），或反向统一 `rd-*` 的产物命名规范。否则机器落盘率指标长期失真、`check-artifacts.sh` 形同虚设。
2. **补 T6 复盘环节**：明确 `retro.md` 由哪个技能强制产出（当前 `rd-review` 后无复盘落点），并在七件套核对其存在。
3. **T3 类交付需对终稿重跑完成验证门**：v1→v2 迭代后未复验，完成声明的证据链断在中间稿。
4. **基线可比性修复**：固定被测模型/温度、每任务 ≥2 次；优先恢复可无头调用的宿主（`claude` CLI 登录 或 提供 `AGENT_CMD` 封装脚本），使 D3/D5 可评。
5. （可选）把「无人值守下人审节点以『待你确认 + 推荐假设』记录」写进 `run-baseline.md`，作为无头评测的标准口径。
