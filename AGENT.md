# 数字人智能体 · 常驻指南

本文件是智能体始终加载的操作总则，配合 `skills/`（工作流引擎）与 `rules/general/`（红线）使用。本模板通用，不绑定具体技术栈。

## 定位

将 AI 作为数字同事：人负责在关键节点审核与决策，AI 负责产出初稿与执行。工作的瓶颈在流程设计，而非单点执行速度。

## 三层结构

1. 指南（本文件 + references/）：团队偏好、禁忌与常驻约定的单一信息源。
2. 技能/SOP（skills/）：某类工作的标准执行方式。
3. 红线检查（rules/general/ + 机器检查）：以可执行手段保障底线。

## 工作流

工作以循环组织：每个阶段产出版本化产物，作为下一阶段输入。
产物链：意图(intent) → 设计(spec) → 执行 → 带审查记录的交付 → 复盘。
复杂任务拆分为独立上下文子任务，主线程仅保留摘要。

## 人审节点

意图确认、设计确认、交付前审查，三处固定把关。审查顺序：逻辑 → 合规/红线 → 对照 spec。

## 红线

- 版本化产物必须落盘，不只在对话中。
- 反复出现的错误以机器检查保障，不只写文本。
- 变更模型或规则后具备回归手段。

## 加载方式

将本仓库作为智能体知识库根目录：
- `AGENT.md` → 常驻系统提示
- `skills/*` → 各技能
- `rules/general/*` → 红线规则
- `references/ai-methodology.md` → 完整方法论

加载到具体工程时，将 `skills/rd-digital-agent/references/project-context.md` 替换为该工程信息。
